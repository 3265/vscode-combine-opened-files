{
  "name": "vscode-combine-selected-open-files",
  "displayName": "Combine Selected Open Files",
  "description": "Combine the content of open files into a single new file.",
  "version": "1.0.3",
  "publisher": "3265",
  "license": "MIT",
  "icon": "resources/logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/3265/vscode-combine-selected-open-files.git"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "combine",
    "merge",
    "concatenate",
    "file",
    "copy"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onView:combine-selected-open-files-view"
  ],
  "contributes": {
    "commands": [
      {
        "command": "combine-selected-open-files.start",
        "title": "Combine Selected Open Files"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "combine-selected-open-files-container",
          "title": "Combine Selected Open Files",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "combine-selected-open-files-container": [
        {
          "id": "combine-selected-open-files-view",
          "name": "Open Files",
          "type": "webview"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "24.3.1",
    "@types/vscode": "^1.85.0",
    "typescript": "5.9.2"
  }
}
